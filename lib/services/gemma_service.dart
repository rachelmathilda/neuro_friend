import 'package:flutter/material.dart';

class GemmaServiceScreen extends StatelessWidget {
  const GemmaServiceScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Gemma Service Screen')));
}

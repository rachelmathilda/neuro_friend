import 'package:flutter/material.dart';

class GroqServiceScreen extends StatelessWidget {
  const GroqServiceScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Groq Service Screen')));
}

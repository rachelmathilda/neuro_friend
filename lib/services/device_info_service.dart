import 'package:flutter/material.dart';

class DeviceInfoServiceScreen extends StatelessWidget {
  const DeviceInfoServiceScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Device Info Service Screen')));
}

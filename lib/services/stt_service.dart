import 'package:flutter/material.dart';

class STTServiceScreen extends StatelessWidget {
  const STTServiceScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('STT Service Screen')));
}

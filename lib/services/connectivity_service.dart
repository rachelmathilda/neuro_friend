import 'package:flutter/material.dart';

class ConnectivityServiceScreen extends StatelessWidget {
  const ConnectivityServiceScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Connectivity Service Screen')));
}

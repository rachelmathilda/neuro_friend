import 'package:flutter/material.dart';

class AIVoiceScreen extends StatelessWidget {
  const AIVoiceScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('AI Voice Screen')));
}

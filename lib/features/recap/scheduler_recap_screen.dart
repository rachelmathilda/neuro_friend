import 'package:flutter/material.dart';

class SchedulerRecapScreen extends StatelessWidget {
  const SchedulerRecapScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Scheduler Recap Screen')));
}

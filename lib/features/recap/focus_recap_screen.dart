import 'package:flutter/material.dart';

class FocusRecapScreen extends StatelessWidget {
  const FocusRecapScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Focus Recap Screen')));
}

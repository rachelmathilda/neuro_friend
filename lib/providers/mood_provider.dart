import 'package:flutter/material.dart';

class MoodProviderScreen extends StatelessWidget {
  const MoodProviderScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Mood Provider Screen')));
}

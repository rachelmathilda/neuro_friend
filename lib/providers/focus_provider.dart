import 'package:flutter/material.dart';

class FocusProviderScreen extends StatelessWidget {
  const FocusProviderScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Focus Provider Screen')));
}

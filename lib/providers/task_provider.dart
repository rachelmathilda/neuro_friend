import 'package:flutter/material.dart';

class TaskProviderScreen extends StatelessWidget {
  const TaskProviderScreen({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Task Provider Screen')));
}
